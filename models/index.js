const CustomerModel = require('./customer');
const OrderModel = require('./order');
const UserProfileModel = require('./user-profile');

const Models = {
    CustomerModel,
    OrderModel,
    UserProfileModel
};

module.exports = Models;
